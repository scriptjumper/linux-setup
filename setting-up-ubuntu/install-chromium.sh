# Update package list.
sudo apt update
# Install chromium.
sudo apt install chromium-browser
# You can now open Chromium from your application list or type the following command at the terminal;
chromium-browser
